beautifulsoup4==4.12.3      # For parsing TREC-format corpus and queries
streamlit==1.35.0           # For the simple web interface
rank_bm25==0.2.2            # For BM25 ranking
scikit-learn==1.5.0         # Optional: for cosine similarity or TF-IDF
numpy==1.26.4               # Needed for BM25 calculations
pandas==2.2.2               # For data handling
pytrec_eval==0.5            # For evaluating IR runs (MAP, nDCG, etc.)
